const ServerRoutes = {
  api: '/api',
  goals: '/goals',
  roadmaps: '/roadmaps',
  auth: {
    base: '/auth',
    magicLink: '/magic-link',
    consumeToken: '/consume-token',
  },
};

export default ServerRoutes;
