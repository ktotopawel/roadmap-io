const ServerRoutes = {
  api: '/api',
  goals: '/goals',
  roadmaps: '/roadmaps',
  auth: {
    magicLink: '/auth/magic-link',
    consumeToken: '/auth/consume-token',
  },
};

export default ServerRoutes;
